<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
		http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd">

	<!-- no security for request to resources -->
	<security:http pattern="/resources/**" security="none" />
	
	<security:http auto-config="true" use-expressions="true" servlet-api-provision="true" >
		<security:intercept-url pattern="/login" access="permitAll"/>
		<security:intercept-url pattern="/forgotPassword" access="permitAll"/>
		<security:intercept-url pattern="/deviceSelection" access="permitAll"/>
		<security:intercept-url pattern="/checkUserId" access="permitAll"/>
		<security:intercept-url pattern="/chat" access="permitAll"/>
		<security:intercept-url pattern="/resources/**" access="permitAll"/>
		<security:intercept-url pattern="/**" access="authenticated"/>
		<security:form-login login-page="/login" login-processing-url="/spring_security_check" default-target-url="/" authentication-failure-handler-ref="customAuthenticationFailureHander" />
		<security:logout invalidate-session="true" delete-cookies="JSESSIONID" logout-success-url="/login" />
		<!-- Spring Security - Session management configuration -->
		<security:session-management invalid-session-url="/login" session-authentication-error-url="/login" session-fixation-protection="newSession">
			<security:concurrency-control error-if-maximum-exceeded="true" max-sessions="1" expired-url="/login"/>
		</security:session-management>
		<!-- Click jack and cross site scripting fix -->
		<security:headers>
			<security:frame-options policy="SAMEORIGIN"/>
			<security:cache-control/>
			<security:xss-protection/>
		</security:headers>
		<!-- HDIV with Spring Security configuration -->
		<security:custom-filter after="SECURITY_CONTEXT_FILTER" ref="hdivFilter" />
	</security:http>
	
	<security:authentication-manager>
		<security:authentication-provider ref="customAuthenticationProvider" />
	</security:authentication-manager>
	
	<!-- Custom Authentication Provider -->
	<bean id="customAuthenticationProvider" class="io.c12.bala.spring.security.auth.CustomAuthenticationProvider" />
	
	<!-- Custom authentication failure handler -->
	<bean id="customAuthenticationFailureHander" class="io.c12.bala.spring.security.auth.CustomAuthenticationFailureHander" />
	
	<security:global-method-security secured-annotations="enabled"></security:global-method-security>
	<!-- Root Context: defines shared resources visible to all other web components -->
	
	<bean id="propertyConfigurer"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="location" value="classpath:security-config.properties" />
	</bean>
	
	<!-- HDIV filter -->
	<bean id="hdivFilter" class="org.hdiv.filter.ValidatorFilter" />

</beans>
